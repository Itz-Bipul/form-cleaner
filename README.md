# Form Color Filter – Keep Only Black Text (Image + PDF)

A Streamlit dashboard that removes all non‑black colours from scanned forms. Supports images (JPG, PNG, TIFF, BMP) and multi‑page PDFs.

## Quick Start

1. **Install dependencies** (preferably in a virtual environment):
   ```bash
   pip install -r requirements.txt